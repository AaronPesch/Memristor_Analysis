# Copied here at startup by analysis_app.py, out of the plotly and Qt packages.
web/plotly.min.js
web/qwebchannel.js
__pycache__/

# The repo root ignores *.html, because the old pipeline generated them by the
# dozen. The pages in web/ are hand-written source, not output.
!web/*.html
